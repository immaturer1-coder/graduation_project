services:
  db:
    image: postgres:15
    volumes:
      - ./tmp/db:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: password

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  mailhog:
    image: mailhog/mailhog:latest
    ports:
      - "1025:1025"
      - "8025:8025"

  web:
    build:
      context: .
      args:
        USER_UID: 1000
        USER_GID: 1000
    command: >
      bash -c "
      rm -f tmp/pids/server.pid &&
      if [ ! -d 'node_modules' ]; then
        npm install &&
        npm install @hotwired/turbo-rails @hotwired/stimulus esbuild tailwindcss @tailwindcss/cli --save
      fi &&
      bundle exec rails s -p 3000 -b '0.0.0.0'
      "
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      POSTGRES_PASSWORD: password
      GRADUATION_PROJECT_DATABASE_PASSWORD: password
      HOST_URL: 192.168.1.4
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      SMTP_HOST: mailhog
    depends_on:
      - db
      - redis
      - mailhog

  proxy:
    image: nginx:alpine
    ports:
      - "443:443"
    volumes:
      - ./certs:/etc/nginx/certs:ro
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - web